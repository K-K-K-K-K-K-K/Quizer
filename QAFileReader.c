QAUnit *getQAs(char *path) {
	QAUnit *qas = malloc(sizeof(QAUnit * ASIZE);

	// ファイル開く

	for (/*ファイルの終わりまで*/) {
		// 一行読み込む
		qas[i] = getQA(line);
	}

	return qas;
}

QAUnit getQA(char *s) {
	// 文字列を分解し、QAUnitに代入し返却
	return;
}

